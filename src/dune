(ocamllex (modules forthlexer))
(menhir (modules menhir_parser))

(executable
  (name nemesis)
  (libraries
    nice_parser
    core
    str
    base
    cmdliner
    logs
    fmt.tty
    fmt.cli
    logs.fmt
    logs.cli
  )
  (preprocess (pps ppx_jane))
)
